# Colors
- [ ] What happens on hover of #primary-navigation?

# General
- [x] Update mobile navigation text to match desktop.
- [x] Different heading colors on mobile and desktop.
- [x] Link mobile menu links to navigate site.
- [x] Footer address font size discrepancy.
- [x] Drop mobile heading to "Ag" on secondary pages too.
- [ ] Determine an aspect ratio for the Orbit images (bottom cutting off on mobile).
- [x] Top margin on small sizes of the footer (shouldn't be same as large size)
- [x] Add app.js to list of concatenated JavaScript files.
- [x] Footer contact info text loses its left spacing on mobile.

# Android
## generic browser
- [ ] Noto Serif not loading in Android browser.
- [ ] Mobile menu isn't working in Android browser.
## Chrome
## Firefox
- [ ] Disable swipe on mobile menu (only works in Firefox, conflicts/breaks the Orbit slider there).

# Internet Explorer
- [x] Fix the broken header wrapping.

---

# Fonts
- [x] $bodyFontStack: "'Helvetica Neue', Helvetica, Arial, sans serif";
- [x] $headingFontStack: "Noto Serif";
- [x] Set headings, Ag Heritage tiles, off-canvas and primary navigation to serif.

# Questions
- [x] Font icons, SVG, or spritesheet?
- [x] Where to include a logo/link back to SDSU?

# Desktop Navigation
- [x] Move margin/padding from LI to UL so second line wraps to match the first.
- [x] Shrink the seal and set into top-bar on medium screens.
- [x] Add top padding above .page-title.

# Mobile Navigation
- [x] List items should hover with an accent color.
- [x] White text.
- [x] Bottom HR spacing.
- [x] Search still lacks a button.
- [x] Look into using horizontal rules as the dividers between off-canvas list items (as opposed to the .divider class there now).
- [x] Remove the seal and fix the title accordingly.

# Homepage
- [x] Collapse the 3 columns to full width on mobile.
- [x] Collapse the double padding between panels to single.
- [x] Figure out why the slider doesn't always load properly (in terms of its height).
- [x] Change Orbit GUI elements to one of Niki's colors.

# Secondary Pages
- [x] Match first side-nav link's top padding/margin to left
- [x] Darken the side-bar link to black
- [x] Match the padding/margin between the breadcrumbs and h1 to what's between the h1 and initial paragraph.
- [x] Line up the start of the breadcrumbs with h1 and paragraph.
- [x] Add a watcher to keep the page navigation and primary content area the same height.

# Page Navigation

# General
- [x] Increase height above headings in footer.

# Footer
- [x] Decrease leading between first-child h1's and ul
- [x] Decrease leading between list items
- [x] Fix awkward break in the contact title

# Tooling
- [x] Implement `copy` task in Grunt to move appropriate assets into build/ and update the `assets` environment variable accordingly.
- [x] Combine Grunt `watch` and `server` tasks into a single command.
- [x] Implement a `clean` task.
